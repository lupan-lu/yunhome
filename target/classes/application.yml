spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql:///yunhome?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: 19800820
  thymeleaf:
    cache: false

# mybatis showsql
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
# pagehelper
pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql